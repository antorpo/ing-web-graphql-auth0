import { UserTypes } from "./users/types";
import { RestaurantTypes } from "./restaurants/types";
import { ReservationTypes } from "./reservations/types";
import { HeadquarterTypes } from "./headquarters/types";
import { TableTypes } from "./tables/types";
import { ScheduleTypes } from "./schedules/types";

const GlobalTypes = [UserTypes, RestaurantTypes, ReservationTypes, HeadquarterTypes, TableTypes, ScheduleTypes];

export { GlobalTypes };
